export interface buttonConstants {
  START: string;
  NEXT: string;
  PREV: string;
  FIN: string;
}

const buttonContent: buttonConstants = {
  START: '시작하기',
  NEXT: '다음',
  PREV: '뒤로',
  FIN: '완료',
};

export default buttonContent;
